print ("Executing /home/matt/.pythonrc")
# enable tab completion in python
try:
    import readline
except ImportError:
    print ("Module readline not available.")
else:
    print ("Enabling tab completion")
    import rlcompleter
    readline.parse_and_bind("tab: complete")

# vim: set ft=python :
